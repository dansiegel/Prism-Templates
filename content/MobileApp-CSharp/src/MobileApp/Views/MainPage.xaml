<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behaviors="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
             <!--#if Localization -->
             xmlns:i18n="clr-namespace:MobileApp.i18n;assembly=MobileApp"
             <!--#endif -->
             xmlns:xaml="clr-namespace:MobileApp.Xaml;assembly=MobileApp"
             BindingContext="{xaml:ViewModelLocator {x:Reference view}}"
             <!--#if UseMvvmHelpers -->
             Title="{Binding Title}"
             <!--#else -->
             Title="Main Page"
             <!--#endif -->
             x:Name="view"
             x:Class="MobileApp.Views.MainPage">

<!--#if Localization -->
    <ListView ItemsSource="{Binding TodoItems}" 
              Header="{i18n:Translate HelloWorld}">
<!--#else -->
    <ListView ItemsSource="{Binding TodoItems}">
<!--#endif -->
        <ListView.Behaviors>
            <behaviors:EventToCommandBehavior Command="{Binding TodoItemTappedCommand}" 
                                              EventName="ItemTapped"
                                              EventArgsParameterPath="Item" />
        </ListView.Behaviors>
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Name}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>
